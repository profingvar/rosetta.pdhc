Current focus
=============

#502 / #569 — openEHR operational templates (.opt) for anthropometry /
lung_function / laboratory. The .opt AUTHORING itself is the external
Archetype-Designer + Phanera-sandbox step (openehr.phanera.se) — NOT code.

PREWORK DONE 2026-08-22 (commit b635466):
  docs/openehr_flat_opt_field_map_502.md — the FLAT↔.opt field-mapping spec.
  Read this first. It gives, per template: flat_root, composition_defaults,
  per-concept target FLAT paths + UCUM, the laboratory repeating-analyte
  cluster pattern, worked FLAT examples, 5 open modelling decisions, and a
  coder checklist. pdhc_concept_map.json gaps now carry draft binding_kind +
  target_flat_path / analyte_cluster_path (not runtime-read).

TWO CODE DELTAS the new templates need (see doc §2), do AFTER the .opt web
templates are captured:
  1. Registry: single `template` object → a per-template `templates` map
     (each with its own flat_root + composition_defaults); Binding carries
     its template's context.
  2. Emitter: grouping key (patient,time) → (patient,time,template_id); add a
     `repeating_analyte` mode for pdhc_laboratory.v1 (one generic
     laboratory_test_result OBSERVATION, repeating analyte CLUSTER keyed by
     analyte_name+LOINC at index :N).

Next-up (order): anthropometry.v1 (smallest, all-standard) → laboratory.v1
(biggest ROI, ~27 concepts, one archetype) → lung_function.v1 (1 concept).
Each: author .opt → capture web template → promote gap bindings → extend
emitter → #507 round-trip on the sandbox → commit .opt + web template.

Resolve with the sandbox owner/clinician first: D-A1 self-reported weight,
D-LF1 FEV1 leaf-vs-cluster, D-L1 analyte terminology (LOINC vs local),
D-L2 counts (cgm_hypo_count), D-L3 panel vs per-analyte compositions.

spo2 (live, DV_PROPORTION) still awaits its own sandbox round-trip before
delivery is enabled — same gate.
